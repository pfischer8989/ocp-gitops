apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: onlineboutique-ingress
spec:
  hosts:
  - "*"
  gateways:
  - onlineb-gateway
  http:
  - name: "firefox"
    match:
    - headers:
        user-agent:
          regex: ".*Firefox.*"
    route:
    - destination:
        host: frontend

